# Deliverable 3 Testing Notes - Davinder Kehal

I pulled the latest Deliverable 3 version from the team repository and tested it on my system.

## Docker Testing
- Docker Desktop was running successfully.
- I ran the project using `docker compose up --build -d`.
- Both containers started properly:
  - app
  - mysql
- The application opened successfully at `http://localhost:8081`.

## Functional Testing
- The home page loaded correctly.
- The login page opened correctly.
- Admin login worked successfully.
- The Events page loaded correctly.
- The Admin panel was accessible after login.

## Notes
The project is working correctly in the Docker environment on my side.
This confirms that Deliverable 3 deployment and MySQL integration are functioning properly.